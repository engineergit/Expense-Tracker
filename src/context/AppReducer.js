//dispatch contains ac action
export default(state, action) => {
    switch (action.type) {
        default:
            return state;   
    }
}
// case 'INCREMENT_TRANSACTION',
//     return{
//         ...state,
//         transactions:[action.payload,...state.transactions]